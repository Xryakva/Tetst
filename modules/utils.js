const fs = require('fs'); const sleep = ms => new Promise(r => setTimeout(r, ms)); const readLines = file => fs.readFileSync(file, 'utf8').split('\n').filter(l => l.trim()); const writeIndex = file => { const idx = parseInt(fs.readFileSync(file, 'utf8')) || 0; fs.writeFileSync(file, String(idx + 1)); return idx + 1; }; const getIndex = file => parseInt(fs.readFileSync(file, 'utf8')) || 0; module.exports = { sleep, readLines, writeIndex, getIndex };