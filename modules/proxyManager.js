class ProxyManager { constructor() { this.proxies = [{ url: 'direct', used: 0, slots: 999 }]; } getAvailableProxy() { return this.proxies[0]; } acquireSlot() { if (this.proxies[0].used < this.proxies[0].slots) { this.proxies[0].used++; return true; } return false; } releaseSlot() { if (this.proxies[0].used > 0) this.proxies[0].used--; } getStatus() { return this.proxies.map(p => ({ url: p.url, used: p.used, slots: p.slots })); } waitForConnect() { return true; } isBlocked() { return false; } blockProxy() {} } module.exports = ProxyManager;